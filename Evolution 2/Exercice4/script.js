const tab = [-2,1,4];
tab.length;

function additionne (x) {
return x + 2;
}

function affiche () {
alert("Le résultat est " + additionne(tab [0]));
alert("Le résultat est = " + additionne(tab [2]));
}